# URL of the trailhead-backend server
VITE_BACKEND_URL=http://localhost:4000

# Which business this frontend instance represents — matches an id in
# the backend's data/businesses.json. Each real business deployment would
# set this to their own id (e.g. via their hosting platform's env vars).
VITE_BUSINESS_ID=trailhead
